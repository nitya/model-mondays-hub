{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://model-mondays-hub/schema/tag.schema.json",
  "title": "Tag",
  "description": "A categorized label applied to episodes (model, technology, or partner).",
  "type": "object",
  "required": ["id", "label", "category"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Slug identifier, matches the YAML filename without extension.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "label": {
      "type": "string",
      "description": "Human-readable display name."
    },
    "category": {
      "type": "string",
      "enum": ["model", "technology", "partner"],
      "description": "Which facet of the taxonomy this tag belongs to."
    },
    "description": {
      "type": "string",
      "description": "Optional one-line explanation of the tag."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "Optional canonical link (e.g. product page, model card)."
    }
  }
}
